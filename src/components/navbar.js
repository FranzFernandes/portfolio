import React from 'react';

const Navbar = () => (
  <div
    id="bar"
    style={{
      display: 'flex',
      maxWidth: 960,
      justifyContent: 'flex-end',
    }}
  >
    <div id="buttons">
      <button type="button"> Test</button>
    </div>
  </div>
);

export default Navbar;
